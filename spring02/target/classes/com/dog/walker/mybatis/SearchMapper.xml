<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="searchMapper">

	<select id="getStarList" parameterType="String" resultType="manageDto">
		SELECT nickname, AVG(stars) FROM manage 
		WHERE nickname 
		IN(SELECT nickname FROM pet_walker WHERE addr LIKE '%' || SUBSTR(#{input_address},1,3) || '%' ) GROUP BY nickname
	</select>
	
	<select id="getFilePath" parameterType="String" resultType="manageDto">
		SELECT filePath FROM manage WHERE id = (SELECT id from pet_walker WHERE addr LIKE '%' || SUBSTR(#{input_address},1,3) || '%' )
	</select>
	
</mapper>